{
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "gh-pages": "^4.0.0",
    "typescript": "^4.9.4"
  },
  "scripts": {
    "build": "tsc && copyfiles -u 1 src/*.html src/*.css build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  }
}
